# `@workspace/sanity`

Shared Sanity client, GROQ queries, and generated types — consumed by `apps/web` only.

`src/sanity.types.ts` is **generated**, never hand-edit it — see the typegen propagation rule in the root `AGENTS.md` and the full walkthrough in `docs/runbooks/sanity-workflow.md`. For what's modeled and how `apps/web` consumes it, see `docs/feature/sanity-cms.md`.
